# Version floors, not exact pins: resilient for anyone cloning while staying
# reproducible. Tested with requests 2.34 / matplotlib 3.10 on Python 3.10.
requests>=2.31
matplotlib>=3.8
numpy>=1.24

# Optional: enables the gradient-boosting rung in forecast_ladder.py.
# Everything else runs without it.
# scikit-learn>=1.3
